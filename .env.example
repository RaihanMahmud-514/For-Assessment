# Copy this file to cypress.env.json (gitignored) OR export as shell env vars
# before running tests. Never commit real credentials.

# Option A: shell env vars (used by cypress.config.js)
EVO_EMAIL=raihanm.mahmud@gmail.com
EVO_PASSWORD=gVGdNDtKBU5ezX6

# Option B: cypress.env.json equivalent
# {
#   "EVO_EMAIL": "your-test-account@example.com",
#   "EVO_PASSWORD": "your-test-account-password"
# }
